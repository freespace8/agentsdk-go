# Deployment

The Go SDK primarily targets embedding into larger systems, but the reference CLI `agentctl` may be shipped as a standalone binary. Capture env flags, configuration layout, and distribution targets here once they exist.
